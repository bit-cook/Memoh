name: Docker PR
on:
  pull_request:
    branches: [main, "v*.*"]
permissions:
  contents: read
concurrency:
  group: docker-pr-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    uses: ./.github/workflows/docker-build.yml
    with:
      publish: false
