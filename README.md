# Computer Use / Browser Use P0 verification screenshots

Evidence captured from the `memoh-cua` development environment (server 19880, web 19882) while the
P0 branch `feat/computer-browser-p0` was running. Files under `shots/` are referenced from the PR body.
